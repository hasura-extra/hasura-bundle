The <info>%command.name%</info> command generates a new Hasura event handler class.

<info>php %command.full_name% productInserted</info>

If the argument is missing, the command will ask for the event name interactively.