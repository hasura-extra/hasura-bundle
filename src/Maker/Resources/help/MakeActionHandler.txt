The <info>%command.name%</info> command generates a new Hasura action handler class.

<info>php %command.full_name% insertProduct</info>

If the argument is missing, the command will ask for the action name interactively.